pipeline {
    agent any

    stages {
        stage('branch name') {
            steps {
                script {
                    echo "print pranch name 4 times"
                    echo "Selected branch: ${env.BRANCH_NAME}"
                    echo "build from webhook"
                    }
                }
            }
        }
    }
