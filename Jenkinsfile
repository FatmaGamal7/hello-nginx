pipeline {
    agent any

    stages {
        stage('branch name') {
            steps {
                script {
                    echo "orint pranch name 3 times"
                    echo "Selected branch: ${env.BRANCH_NAME}"
                    echo "Selected branch: ${env.BRANCH_NAME}"
                    echo "Selected branch: ${env.BRANCH_NAME}"
                    }
                }
            }
        }
    }
