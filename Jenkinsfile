pipeline {
    agent any

    stages {
        stage('branch name') {
            steps {
                script {
                    echo "Selected branch: ${env.BRANCH_NAME}"
                    }
                }
            }
        }
    }
