pipeline {
    agent any

    stages {
        stage('branch name') {
            steps {
                script {
                    echo "Selected branch: dev"
                    echo "build from webhook #5"
                    }
                }
            }
        }
    }
